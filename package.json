{
  "name": "jobjockey",
  "version": "1.0.0",
  "private": true,
  "description": "JobJockey — browser automation that rides through job portals (starting with Dice Easy Apply) and applies on your behalf using Playwright.",
  "type": "module",
  "scripts": {
    "login": "node src/login.js",
    "apply": "node src/applyDice.js",
    "apply:auto": "node src/applyDice.js --auto-submit"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
